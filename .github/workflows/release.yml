name: Release

on:
  push:
    tags:
      - 'v*'  # Triggers on tags like v1.0.0, v1.2.3, etc.

jobs:
  build:
    name: Build Cross-Platform Binaries
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install fyne-cross
        run: go install github.com/fyne-io/fyne-cross@latest

      - name: Install dependencies
        run: go mod download

      - name: Build for Windows
        run: |
          fyne-cross windows -arch=amd64 -app-id=com.subman.app

      - name: Build for macOS (Intel)
        run: |
          fyne-cross darwin -arch=amd64 -app-id=com.subman.app

      - name: Build for macOS (Apple Silicon)
        run: |
          fyne-cross darwin -arch=arm64 -app-id=com.subman.app

      - name: Build for Linux
        run: |
          fyne-cross linux -arch=amd64 -app-id=com.subman.app

      - name: Prepare release assets
        run: |
          mkdir -p release
          # Windows
          if [ -f fyne-cross/dist/windows-amd64/subman.exe ]; then
            zip -j release/subman-windows-amd64.zip fyne-cross/dist/windows-amd64/subman.exe
          fi
          # macOS Intel
          if [ -d fyne-cross/dist/darwin-amd64/subman.app ]; then
            cd fyne-cross/dist/darwin-amd64
            zip -r ../../../release/subman-macos-amd64.zip subman.app
            cd ../../..
          fi
          # macOS Apple Silicon
          if [ -d fyne-cross/dist/darwin-arm64/subman.app ]; then
            cd fyne-cross/dist/darwin-arm64
            zip -r ../../../release/subman-macos-arm64.zip subman.app
            cd ../../..
          fi
          # Linux
          if [ -f fyne-cross/dist/linux-amd64/subman ]; then
            tar -czf release/subman-linux-amd64.tar.gz -C fyne-cross/dist/linux-amd64 subman
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
